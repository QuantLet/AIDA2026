# AIDA Summer School 2026 -- August course. CHRONOS environment.
#
# Regenerates precomputed/chronos_bolt_*.csv and precomputed/chronos_t5_*.csv, i.e.
# stage 02 (src/02_chronos_lab.py). This is `CHRONOS_PY` in run_all.sh, which points at
# ../aida-ensemble/venv/bin/python; the versions below are that interpreter's.
#
# It is a SEPARATE environment on purpose. chronos-forecasting pins a transformers
# generation that the open-weights stages have since moved past, and the deck's own
# argument is that a foundation model's capability belongs to a specific artefact and
# interface -- so the two stacks are pinned apart rather than reconciled.
#
#   python 3.13.9, macOS 26.6.1, Apple M5 Max (arm64), CPU only, no CUDA
#
# MODEL ARTEFACTS, which matter as much as the package versions and are named on the
# slides for that reason:
#   amazon/chronos-t5-mini     sampling head, 500 paths (2000 for the rerun comparison)
#
# amazon/chronos-bolt-tiny is reachable from the same environment and src/02_chronos_lab.py
# still has the `bolt` path, but it is scored nowhere: its trained grid starts at 0.1, so
# it has no number to contribute at the 1% this course reports.
#
chronos-forecasting==2.2.2
transformers==4.57.6
accelerate==1.13.0
huggingface-hub==0.36.2
tokenizers==0.22.2
safetensors==0.7.0
torch==2.12.0

numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
matplotlib==3.10.9

# Stage 01 refreshes the lab dataset from the certified aida-risk run and can fall back
# to a download; the core environment does not carry it.
yfinance==1.3.0
